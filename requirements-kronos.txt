# The text agent and app discovery use the Python standard library.
# Optional features: OS credential storage, recoverable trash, Windows volume.
keyring==25.7.0
Send2Trash==1.8.3
pycaw==20251023; sys_platform == "win32"
comtypes==1.4.16; sys_platform == "win32"
# Native credential setup; imported only when the setup window is opened.
PyQt6==6.8.1
PyQt6-Qt6==6.8.2
